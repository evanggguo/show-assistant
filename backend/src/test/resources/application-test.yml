spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://localhost:5432/showassistant_test}
    username: ${TEST_DB_USERNAME:postgres}
    password: ${TEST_DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  ai:
    anthropic:
      api-key: test-key-placeholder

app:
  sse:
    timeout-ms: 30000
